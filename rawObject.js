export default class RawObject {
    width;
    height;
    x;
    y;
    constructor(x, y, width, height) {
        this.width=width;
        this.height=height;
        this.x=x;
        this.y=y;
    }
}